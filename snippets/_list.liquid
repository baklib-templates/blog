{% comment %} List row for index.list — aligned with card/search typography {% endcomment %}
{% assign page_thumb_image_url = page.settings.thumb_image_url | presence %}
<article class="flex gap-4 border-b border-base-300 px-4 py-6 transition-colors last:border-b-0 hover:bg-base-200/50 ">
  <div class="min-w-0 flex-1">
    <a href="{{ page.path }}" class="group block">
      <h2 class="text-lg font-semibold leading-snug tracking-tight text-accent group-hover:text-base-content/70 md:text-xl">
        {{ page.link_text }}
      </h2>
      {% if page.settings.description %}
        <p class="mt-2 line-clamp-2 text-sm leading-relaxed text-base-content/60 md:text-[15px]">
          {{ page.settings.description }}
        </p>
      {% endif %}
    </a>

    {% assign tag_count = page.tags | size %}
    {% if tag_count > 0 %}
      <ul class="mt-3 flex flex-wrap gap-2">
        {% for tag in page.tags %}
          <li>
            <a
              href="{{ tag.path }}"
              class="inline-flex rounded-selector bg-base-200 px-2.5 py-0.5 text-xs font-medium text-base-content/80 transition-colors hover:bg-base-300"
            >
              {{ tag.name }}
            </a>
          </li>
        {% endfor %}
      </ul>
    {% endif %}

    {% render 'sub_page_list', page: page, per: 5 %}
  </div>

  {% if page_thumb_image_url %}
    <a href="{{ page.path }}" class="group/thumb relative shrink-0" aria-hidden="true" tabindex="-1">
      <span class="block h-20 w-28 overflow-hidden rounded-box border border-base-300 bg-base-200 sm:h-24 sm:w-36">
        <img
          src="{{ page_thumb_image_url }}"
          alt=""
          class="h-full w-full object-cover transition duration-300 group-hover/thumb:scale-[1.03]"
          loading="lazy"
        />
      </span>
    </a>
  {% endif %}
</article>
