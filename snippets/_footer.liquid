{% assign footer_menu_html = site.settings.footer_menu_html | presence %}

{% if footer_menu_html %}
  <footer
    id="footer"
    class="footer footer-center bg-accent p-6 text-sm text-accent-content"
  >
    <div class="justify-between items-center space-y-2">
      {% # 自定义导航菜单 %}
      <div class="flex justify-center space-x-3">
        {{ footer_menu_html }}
      </div>
    </div>
  </footer>
{% endif %}
